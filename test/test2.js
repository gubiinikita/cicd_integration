const chai = require('chai');
const assert = chai.assert;

describe('files 2', function () {
  describe('export 2', function () {
    it('should export pdf 2', async function () {
      assert.isTrue(true);
    });

    it('should export html 2', async function () {
      assert.isTrue(true);
    });

    it('should export yml 2', async function () {
      assert.isTrue(true);
    });

    // it('should export text 2', async function () {
    //   throw new Error('An exception occurred');
    // });
  });
});