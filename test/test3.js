const chai = require('chai');
const assert = chai.assert;

describe('files 3', function () {
  describe('export 3', function () {
    it('should export pdf 3', async function () {
      assert.isTrue(true);
    });

    it('should export html 3', async function () {
      assert.isTrue(true);
    });

    it('should export yml 3', async function () {
      assert.isTrue(true);
    });

    // it('should export text 3', async function () {
    //   throw new Error('An exception occurred');
    // });
  });
});